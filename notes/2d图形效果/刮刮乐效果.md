# 刮刮乐效果

## 原理
核心思路是双层遮罩 + 触摸擦除：底层显示中奖内容，上层是不透明的刮刮遮盖层，通过触摸事件擦除上层指定区域，露出底层内容。

图层结构：
底层（Content）：显示中奖图片 / 文字（如 “谢谢参与”“100 元”）。
上层（MaskLayer）：不透明的遮盖层（灰色 / 磨砂纹理），作为 “可刮擦区域”。

擦除逻辑：
使用 RenderTexture（渲染纹理）承载遮盖层，触摸时在 RenderTexture 上绘制透明区域（模拟刮擦），最终通过 Sprite 渲染该 RenderTexture，实现 “擦除” 视觉效果。

